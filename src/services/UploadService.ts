import * as multer from 'multer';
import { Service } from './service';

export class UploadService extends Service {
  // TODO: Save the image url with user-id into our persistence layer, PostgreSQL database.

}
